#/bin/bash

if [ ! $1 ] ; then
	echo ''
	echo 'usage: '
	echo 'add name_of_the_program text_to_commit'
	echo ''
	exit
fi

git add $1

if [ $2 ] ; then
	git commit -m $2
else
	git commit -m "added $1"
fi

git push

